<Window x:Class="DCSBIOSDataBroker.Windows.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="About DCS-BIOS Data Broker" Height="400" Width="600" Loaded="WindowLoaded" Icon="/images/app_image.jpg" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image Name="ImageSchema"  Source="/images/broker_schema.png" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"  Grid.RowSpan="3" Tag="False" MinWidth="450" RenderOptions.BitmapScalingMode="Fant" IsEnabled="False" />
        
        <WrapPanel Orientation="Vertical" Grid.Column="1" Grid.Row="3" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="10,10,10,10">
            <Button Name="ButtonOk" Content="Ok" VerticalAlignment="Bottom" Width="70" Click="ButtonOk_OnClick"></Button>
        </WrapPanel>

    </Grid>
</Window>
